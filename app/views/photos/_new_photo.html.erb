<% if user == current_user %>
  <%= form_for :photo, user_photos_path(user), 
  :html => { :multipart => true } do |f| %>
    <%= f.file_field :image %>
    <%= f.submit "send photo" %>
    <% end %>
<% end %>